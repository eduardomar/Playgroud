{
  "name": "playgroud",
  "version": "1.0.0",
  "description": "Mi version para hacer mi cagadero",
  "main": "dist/index.js",
  "scripts": {
    "start": "concurrently \"tsc -w\" \"tsc-alias -w\"",
    "playground": "tsc && node dist/index.js",
    "build": "tsc && tsc-alias",
    "build:watch": "tsc -w && tsc-alias -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.3.2"
  },
  "devDependencies": {
    "@types/debug": "^4.1.6",
    "concurrently": "^6.2.0",
    "tsc-alias": "^1.3.7",
    "typescript": "^4.3.5"
  }
}
